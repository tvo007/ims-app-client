export const API_URL = process.env.REACT_APP_API_URL;

export const HTTP_STATUS = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});

export const LOGIN_STATUS = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});

export const ADD_SKU = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});

export const DELETE_SKU = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});

export const ADD_PRODUCT = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});

export const DELETE_PRODUCT = Object.freeze ({
  PENDING: 'PENDING',
  FULFILLED: 'FULFILLED',
  REJECTED: 'REJECTED',
});
